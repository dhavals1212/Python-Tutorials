name = input('What is your name? ')
#Here 'input' is a command to get input from the user.
#input has to be assigned to a variable, here that variable is 'name'.
#In input, after the text written in '' there has to be a space left for user input.

color = input('What is your favorite color? ')

print(name + ' likes ' + color + ' color.')
#We can use either "" or '' to denote the text inside the command.
#Here the 'name' is a variable that's used in print command, to get its value from previously stored input.
#to use a variable input in a text it has to be added with '+' sign with adequate spacing.
#'print' function simply prints or displays what the code has written.
